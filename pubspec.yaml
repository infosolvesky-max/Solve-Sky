name: solvesky_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # প্যাকেজগুলো স্থিতিশীল (stable) রাখার জন্য ভার্সন নম্বর যোগ করা হলো।
  cupertino_icons: ^1.0.2
  webview_flutter: ^4.7.0

  # ফাইল আপলোডের কার্যকারিতা চালু করার জন্য এই দুটি প্যাকেজ যোগ করা হলো।
  webview_flutter_android: ^3.16.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
