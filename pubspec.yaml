name: solvesky_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # প্যাকেজগুলো স্থিতিশীল (stable) রাখার জন্য ভার্সন নম্বর যোগ করা হলো।
  cupertino_icons: ^1.0.2
  
  # *** এখানে পরিবর্তন করা হয়েছে ***
  # সংস্করণ নম্বরগুলো মুছে দেওয়া হয়েছে যাতে সর্বশেষ সংস্করণ ডাউনলোড হয়।
  webview_flutter:
  webview_flutter_android:

  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true
